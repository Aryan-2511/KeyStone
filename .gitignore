# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# uv / virtualenv
.venv/
# NOTE: uv.lock IS committed — do not ignore it.

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Type / lint caches
.mypy_cache/
.ruff_cache/

# Tooling
.pre-commit-cache/

# Secrets & local env (never commit)
.env
.env.*
*.pem
*.key
secrets.*

# Evidence ledger / generated artifacts (synthetic, regenerable)
*.db
*.sqlite
*.sqlite3

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# FINETUNE-SPIKE-01: chat dataset is generated (embeds the full system prompt x465, ~1.3MB);
# reproducible from the committed train.jsonl via `make finetune-data`.
src/keystone/finetune/data/train_chat.jsonl

# FT-EVAL: the fine-tuned model weights are multi-GB binaries — NEVER commit them.
# The GGUF (~3.1GB) and the merged HF checkpoint (~2GB) are training artifacts, not source.
# The *Modelfile* IS committed (it documents the exact on-prem inference conditions).
*.gguf
keystone-triage-ft-merged/
