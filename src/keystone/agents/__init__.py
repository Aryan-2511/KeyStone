"""Orchestration / agents — NeMo Agent Toolkit (`nvidia-nat`) workflows.

The orchestration entry point. May depend inward on the deterministic core;
the core must not depend on it. Empty until Phase 1.
"""
